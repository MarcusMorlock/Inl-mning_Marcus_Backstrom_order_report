[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "order-report"
version = "0.1.0"
description = "Ett litet exempelpaket för temperaturberäkningar"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]